#!/bin/bash
#######################################
# Local Filesystem Configuration Example
# Use this configuration for local testing without S3
#######################################

# Spark Configuration
# --------------------
# Spark master URL for local testing
SPARK_MASTER="local[*]"

# Memory settings - Smaller for local testing
DRIVER_MEMORY="8g"
EXECUTOR_MEMORY="8g"
DRIVER_MAX_RESULT_SIZE="4g"

# Executor settings (not used in local mode)
EXECUTOR_INSTANCES="4"
EXECUTOR_CORES="4"

# Spark SQL settings
SPARK_SHUFFLE_PARTITIONS="200"
SPARK_DEFAULT_PARALLELISM="200"

# Data Generation Configuration
# ------------------------------
# Number of days of data to generate (reduced for local testing)
DAYS_TO_GENERATE="1"  # Just 1 day for local testing

# Data volume per 5-minute bucket (reduced for local testing)
GB_PER_BUCKET="1"  # 1GB per bucket for local testing

# Number of part files per bucket
PARTITIONS_PER_BUCKET="10"  # Fewer partitions for local

# Number of records per bucket (1M records ≈ 1GB with 1KB/record)
RECORDS_PER_BUCKET="1000000"

# Local Path Configuration
# -------------------------
# Base path for Delta Lake tables on local filesystem
DELTA_BASE_PATH="/tmp/delta-lake-bmt"

# Disable all S3 configurations for local mode
# ---------------------------------------------
S3_BUCKET=""  # Empty = use local filesystem
AWS_ACCESS_KEY=""
AWS_SECRET_KEY=""

# Local Optimizations
# --------------------
# No S3 committer needed for local filesystem
S3_COMMITTER_ENABLED="false"

# Smaller Parquet settings for local
PARQUET_BLOCK_SIZE="134217728"  # 128MB
PARQUET_PAGE_SIZE="1048576"  # 1MB
PARQUET_COMPRESSION="snappy"

# Standard Delta Lake settings for local
DELTA_LOG_STORE_CLASS=""  # Use default for local
DELTA_CHECKPOINT_INTERVAL="10"

# Notes for Local Testing
# ------------------------
# This configuration is optimized for local testing with:
# - Reduced data volume (1GB per bucket instead of 20GB)
# - Only 1 day of data (288 buckets instead of 1,152)
# - Total data: ~2.88TB instead of 23TB
# - Suitable for development and testing on a single machine
#
# To use this configuration:
# cp conf/local-example.conf conf/generator.conf
# ./bin/start-generator.sh